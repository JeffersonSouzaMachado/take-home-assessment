import 'dart:ui';

class AppColors {
  // Common
  static const primary = Color(0xFF4EB1FF);
  static const positive = Color(0xFF2FFC8E);
  static const negative = Color(0xFFFF4E4E);

  // Dark
  static const darkBackground = Color(0xFF0A0F1F);
  static const darkSurface = Color(0xFF121826);
  static const darkText = Color(0xFFE6E6E6);

  // Light
  static const lightBackground = Color(0xFFF5F7FA);
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightText = Color(0xFF1A1A1A);
}
